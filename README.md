# Advanced-MX-Records-emails-cleaner
clean your email lists at m,x records.  removes duplicates emails, invalid emails, dead emails addresses from your klist of emails. This does not do smtp check for each adddresses

python smtp_peek_emails.py --list final_cleaned.txt --workers 10 --skip-smtp --output mx_only
